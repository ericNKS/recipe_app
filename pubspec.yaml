name: recipe_app
description: "app para adicionar receitas"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3+1 # Para o banco de dados SQLite
  path_provider: ^2.1.3 # Para encontrar o caminho do banco de dados
  provider: ^6.1.2 # Para gerenciamento de estado (saber quem está logado)
  image_picker: ^1.1.2 # Para selecionar a foto da receita
  path: ^1.9.0 # Para construir caminhos de arquivo
  sqflite_common_ffi: ^2.3.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true
